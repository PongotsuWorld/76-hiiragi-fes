import CueSenderPage from "../../components/cuesender-page";

export const metadata = {
  title: "HOME",
};

const CueSender = () => {
  return(
    <CueSenderPage />
  )
};

export default CueSender;
